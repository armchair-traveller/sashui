export function Switch(el: any, checked?: boolean): {
    update: (curChecked?: boolean) => void;
    destroy: Function;
};
export function toggle(el: any, checked?: boolean): {
    update: (curChecked?: boolean) => void;
    destroy: Function;
};
